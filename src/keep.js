/**
 * <input type="checkbox"
                            name=""
                             id=""
                             checked={todo.checked}
                             onChange={() => setChecked(!checked) }
                              /> 
 */

                              /**
 * input type="checkbox"
                    checked = {item.checked}
                    onChange={()=> handleClick(item.id)}

                    />
 */